# 个人工作台 · 部署与更新说明

## 新地址（稳定访问入口）

| 项目 | 地址 |
| --- | --- |
| **线上访问** | https://j2pm8zdjr9-maker.github.io/workbench/ |
| 源码仓库 | https://github.com/j2pm8zdjr9-maker/workbench |

部署方式：**GitHub Pages**，发布自 `main` 分支根目录。每次向 `main` 推送都会自动重新构建并上线，地址长期固定，不再像临时隧道那样随机失效。

> 旧的 cloudflared 临时隧道地址（enemies-tops-pet-surgeons.trycloudflare.com）建议弃用，统一使用上面的 GitHub Pages 地址。

---

## 本次更新的 5 项内容

1. **部署方式升级**：从临时隧道改为 GitHub Pages，地址稳定可长期访问。

2. **喝水记录交互重设计**：首页与身材管理页的喝水明细改为**折叠展示**——默认仅显示最近 5 条，底部提供「展开全部 (N 条)」按钮按需展开，界面不再被长列表拉爆。

3. **喝水量预设管理**：新增「喝水预设」管理（身材管理 → 喝水 → 预设管理），可自由增删 ml 预设（默认 `150 / 200 / 250 / 300 / 350 / 500`），并且**自动同步到首页「记喝水」快捷胶囊**，改一处两处同时生效。

4. **分类筛选改进**：
   - 分类管理中新增「直接显示分类数量」设置，默认 `3`，可调范围 `2–10`。
   - 筛选条布局：`全部` + 前 `N-1` 个常用分类 + 第 `N` 个**「灵活分类」胶囊**。
   - 点击灵活胶囊弹出剩余分类（首项为「无分类」用于取消筛选）；选中后该胶囊改名显示所选分类，可再次点击重选。常用分类排前，有限屏幕显示更多内容。

5. **历史记录行距优化**：充电 / 加油等历史记录项的行距进一步收紧（`gap: 8px`、`padding: 10px 14px`），比此前更紧凑。

---

## 验证情况

- Playwright 端到端回归测试：**11 / 11 全部通过**（在线上 GitHub Pages 环境实测）。
- 覆盖：刷新后数据持久化、喝水预设默认值与 UI 增删同步首页胶囊、喝水明细折叠/展开、分类灵活胶囊与「无分类」取消、catPin 默认 3、时间段无「自定义」胶囊。

## 使用提示

- 数据仍保存在**浏览器本地**（localStorage，key：`workbench.v1`）。换设备或清理缓存前，请使用应用内的备份功能导出。
- 后续若需继续改功能，直接修改 `/workspace` 文件后 `git push` 到 `main` 即可自动上线。

---

## 第二轮更新（待办 / 分类 / 影剧书）

1. **任务待办新增「未完成」统计与一键归档**：任务待办面板统计栏新增「未完成」项（代表逾期且无法补办的事，与「已完成」区分）；折叠态任务行内新增「标未完成」按钮，点一下直接归档到未完成列表，**不会**像点圆圈那样误标为已完成。

2. **分类「直接显示数量」改为按命名空间独立**：原先是全局共用一个数值，现在每个分类管理（如纪念日、任务标签、影剧书标签等）各自维护独立的「直接显示分类数」，在某处调整只影响该分类，其他分类保持默认 3，互不影响。

3. **影剧书页码管理 + 归档按分类筛选**：
   - 书籍记录新增「总页数 / 当前读到第几页」字段，列表、详情、年度归档中均显示页码进度（如 `📄 120 / 300 页 · 40%`）；展开项有「记页码」按钮可随时更新。
   - 年度归档新增电影 / 电视剧 / 书籍分类筛选，并在归档汇总中增加「读书页数」统计。

> 电影、电视剧、书籍三类本就是影剧书的类型分类，本轮在「年度归档」里补充了按这三类筛选的能力。

---

## 第三轮更新（全面验证 + 移动端顺手优化）

### 验证结论

对全站 17 个模块在 **iPhone 尺寸视口（390×844）** 下做了全面审计：遍历所有模块、填充真实数据跑核心交互、检测横向溢出、捕获控制台/页面异常，并在 1.15 倍字体缩放下复测。

- **未发现功能 bug**：标未完成、圆圈完成、分类 pin 独立、书籍页码、首页喝水、侧边栏抽屉、字体缩放均正常。
- **无控制台错误 / 页面异常**。
- **无横向溢出**，1.15 倍缩放后所有模块仍未出现横向滚动。

### 移动端体验优化

1. **解除缩放限制**：移除 `maximum-scale=1.0, user-scalable=no`，手机浏览器可以自由缩放，更符合无障碍与用户使用习惯。
2. **消除双击延迟**：给按钮、标签、导航、快速入口等交互元素统一加上 `touch-action: manipulation`，消除移动端常见的 300ms 点击延迟。
3. **禁用误触文本选择 / iOS 长按呼出**：对 `.tap`、`.btn`、`.pill`、`.tab`、`.chip`、`.nav-item`、`.qbtn`、`.ov`、`.icon-btn`、`.check`、`.link-btn`、`.opt-row`、`.home-pill`、`.top-fab`、`.item` 等加上 `-webkit-touch-callout: none` 与 `user-select: none`，点按更像原生 App；同时保留 `.input`、`.textarea`、`.select`、`.item-title`、`.item-note`、日记正文等内容区的可选/可长按。
4. **弹窗滚动顺滑**：`.modal` 主体增加 `-webkit-overflow-scrolling: touch`，iOS 上底部抽屉内滚动更跟手。

---

## 第四轮更新（深度代码审查 + Bug 修复）

本轮在前几轮"未发现功能 bug"的基础上做了**逐文件深度代码审查**，并配合移动端真实数据交互审计，定位并修复了若干确凿缺陷：

1. **影剧书「年度归档」分类筛选失效（功能级 bug）**：原实现把已按单一类型过滤的列表传入归档函数，导致在「电影/电视剧」分页下点「书籍」筛选永远为空。已改为传入全量媒体列表，使电影 / 电视剧 / 书籍筛选可跨类型生效。

2. **任务操作缺少空守卫（崩溃风险）**：`done` / `doing` / `del` 在对任务 id 取数据后未判断是否存在，陈旧渲染或并发删除后点击会抛 `TypeError` 崩溃。已补 `if (!x) return;` 兜底。

3. **书籍页码百分比越界**：当前页超过总页数时显示 `>100%`（如 `117%`）。已用 `Math.min(100, …)` 夹紧到 100%。

4. **周期任务下一期提前出现**：完成周期任务生成的新一期 `created` 设为当天，导致在到期日前就显示在日视图。已改为设为到期日，下一期只在到期日才出现。

5. **移动端体验再打磨**：全局加 `-webkit-tap-highlight-color: transparent`（消除 iOS/Android 点击灰闪）、`-webkit-text-size-adjust: 100%`（禁止系统自动缩放文字）；表单输入保持 16px 以规避 iOS 聚焦自动缩放。

---

## 第五轮更新（打卡/任务/各模块展示窗口与操作 bug）

1. **打卡统计方框缩小**：移动端下统计方框改为 **一行四个**，紧凑显示；删除「完成进度百分比」进度条，改为仅保留「今日完成 X/Y」简洁文字，避免空白。

2. **打卡历史删除同步主列表**：在历史记录里删除打卡任务后，主页面「我的打卡任务」列表**立即同步刷新**，不再残留已删除任务且不可操作。

3. **任务统计方框缩小**：与打卡一致，任务待办面板等模块的统计方框按移动端适配缩小到合适尺寸。

4. **任务折叠不误标「未完成」**：折叠行内不再直接放「标未完成」按钮（容易在展开/折叠时误触）；「标为未完成」只在展开视图内作为明确操作按钮存在。标记未完成仍会取消完成状态并归档到「未完成」列表。

5. **任务日历「今天」回到当天**：点击任务日历的「今天」按钮后，任务列表的当前查看日期也会回到今天。

6. **工作留痕 / 影剧书 / 备考计划展示窗口同步缩小**：与打卡/任务使用同一套统计方框样式，均按移动端一行四个紧凑显示。

### 验证

- 本地回归：既有功能 **11/11** + 新功能 **15/15** + 深度修复 **8/8** + 本轮 UI/bug 修复 **13/13**，全部通过。
- 专项覆盖：打卡历史删除后同步、进度条移除、统计框 4 列布局、任务折叠不误归档、展开后仍可标未完成、任务日历回到今天、各模块统计框统一。

---

## 更新：基础字体 115% + 显示适配

- **基线字体调至 115%**：CSS `--font-scale` 默认值由 `1` 改为 `1.15`，`html { zoom }` 整体放大（含弹窗），更易读；设置页「字体大小」滑块默认即 115%，上限由 150% 提升至 175% 留足调节空间。
- **已有数据一次性迁移**：`Store.load` 增加一次性迁移，将历史默认的 `1.0` 统一升级到 `1.15`（用 `_fsMigrated` 标记，仅执行一次，不覆盖用户后续手动调整）。
- **显示适配**：115% 下复用既有的真实视口变量（`--app-vh/--dvh/--vw`、`--*vh-real` 等），缩放后侧边栏/弹窗高度仍精确贴合屏幕；经 iPhone 视口（390×844）遍历全站 17 个模块实测，**无横向溢出**，无控制台错误，刷新后持久化保持 115%。

