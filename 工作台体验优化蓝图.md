# 个人工作台 · 体验优化蓝图

> 目标：平台要"能用且好用"，长期自用。本蓝图对标主流优秀产品（钱迹/随手记记账、滴答清单待办、HabitKit/小日常打卡、Notion/Todoist 空状态与命令面板），把「大功能已定型」之后的「细节打磨」拆成可落地的批次。
> 原则：**低风险、高频受益优先**；通用能力一次做好，全模块共享；不碰数据结构，保证现有数据不丢。

---

## 当前架构（已具备的良好基础）
- 统一设计语言：暖色系（米色 + 橄榄绿 + 赤陶橙）、卡片 + 网格统计 + 列表 + 底部弹窗（sheet）。
- 通用组件齐全：`UI.card / head / stat / stats / tabs / pills / ops / bar / ring / line / bars2 / hbars / form / sheet / confirm / table / pager / badge`。
- 已验证的体验细节：字体缩放、iOS 视口变量、图表点击放大、智能单位缩写（万/亿）、底部 sheet、条件字段、删除二次确认。

---

## 第一批：全站基础体验统一（建议优先，所有模块都受益）

### 1. 空状态升级
- **现状**：`UI.empty` 只有图标 + 一行字。
- **标杆**：Notion / Todoist 的空状态会说明「这是什么、为什么用、怎么开始」，并带一个主按钮。
- **落地**：扩展 `UI.empty` 支持标题/说明/引导按钮；各模块首页空状态加一句引导 + 「+ 新建」按钮。
- **影响文件**：`ui.js`、`mod-*.js` 各模块空态。

### 2. 微交互与动效
- **标杆**：HabitKit「惊艳的图表和动画」、iOS 原生回弹。
- **落地**：
  - 卡片/按钮点击反馈更明显（已有 `.tap`，加 `active` 缩放 + 水波纹）。
  - 数字变动动画（金额、进度条、统计数字滚动到目标值）。
  - 列表新增/删除的进场与退场动画；弹窗 spring 缓动。
- **影响文件**：`app.css`（新增缓动变量与动效类）。

### 3. 加载与骨架屏
- **现状**：首屏与大数据模块无加载反馈。
- **落地**：首屏渲染、图表/长列表加载时用骨架占位；「已保存」状态更细腻（保存中/已保存/失败重试）。
- **影响文件**：`app.css`、`core.js`（save 状态）、`ui.js`。

### 4. 表单体验（高频痛点）
- **标杆**：钱迹「超快捷记账」、滴答清单快速捕获。
- **落地**：
  - 打开表单自动聚焦首个输入框。
  - 金额输入：大数字键盘 + 快捷数额胶囊 + 计算器式即时计算。
  - 日期选择：快捷项「今天 / 昨天 / 本周 / 本月 / 上月」。
  - 输入即时校验（红框 + 行内提示），保存按钮常驻底部。
- **影响文件**：`ui.js`（`UI.form`）、各模块表单字段。

### 5. 导航与全局搜索
- **标杆**：Linear / 效率工具的命令面板。
- **落地**：
  - 左侧 17 个模块导航支持分组（生活 / 记录 / 财务 / 关系 / 设置）、常用置顶、搜索过滤。
  - 顶部加全局搜索（跨模块搜标题/内容/金额/姓名）。
- **影响文件**：`index.html`、`app.css`、`core.js`（导航构建）、新增 `search.js`。

### 6. 手势操作
- **标杆**：iOS 列表左滑操作（编辑/删除/置顶）。
- **落地**：列表项左滑出现操作按钮；关键列表支持下拉刷新。
- **影响文件**：`app.css`、`core.js`（事件委托）、`ui.js`（列表项结构）。

### 7. 无障碍
- 弹窗打开时聚焦首个可操作元素、关闭后焦点回到触发处；补充 `aria-label`；尊重 `prefers-reduced-motion`（关闭动画偏好）。
- **影响文件**：`ui.js`（modal/sheet）、`app.css`。

---

## 第二批：重点模块打磨（按你使用的频率排）

### 记账（对标钱迹/随手记）
- **预算**：月度预算 + 进度环 + 超支提醒。
- **图表增强**：悬停 tooltip、环比/同比、更友好的坐标轴。
- **快速记账**：最近分类记忆、备注快捷短语、金额大键盘。
- **资产**：净资产趋势、账户分组、负债与信用卡单独标识。

### 打卡（对标小日常/HabitKit）
- **连续打卡（streak）**：可视化、日历热力图、记录保持的「小庆祝」动画。
- **提醒**：按习惯设置提醒时间。

### 待办（对标滴答清单）
- **自然语言日期**：输入「明天」「周五」「下周一」自动解析。
- **智能列表**：今天 / 即将到期 / 已逾期 / 全部。
- **优先级 + 子任务 + 标签**。

### 影剧书 / 日记 / 备考
- 影剧书：评分可视化、年度统计、「在读/在看」分组。
- 日记：日历视图、心情标签。
- 备考：倒计时 + 进度、科目热力。

---

## 第三批：进阶层（需要你拍板是否要）

- **深色模式**：CSS 变量切换 + 跟随系统（手机夜间自动变暗）。
- **⌘K 命令面板**：键盘唤起，全局跳转/快捷记账/新建。
- **多端同步状态可视化**：自动备份的进度、上次成功时间、失败原因。
- **报表导出增强**：PDF/Excel 报表美化（已有导出能力）。

---

## 建议的推进节奏
1. **先落地第一批「全站基础体验统一」**——一次做好，17 个模块同时变好，风险最低，体验提升最明显。
2. 再按你的实际使用频率，挑 1–2 个重点模块（记账 / 打卡 / 待办）做第二批。
3. 第三批按需开启。

> 每一批都会：本地验证（无 JS 报错）→ 提交 → 待 GitHub 恢复后自动推送上线。
